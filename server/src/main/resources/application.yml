server:
  port: 8080
  servlet:
    encoding:
      charset: UTF-8
      enabled: true

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gugarden?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: qwe123
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 50MB

app:
  jwt:
    secret: gugarden_jwt_secret_dev_key_2025
    expiration: 604800000  # 7 days in ms
  upload:
    dir: ./uploads
  client-url: http://localhost:3000

  # Naver OAuth
  naver:
    client-id: GLy3tEPCuVKrkgD7bmaw
    client-secret: 7EzKnLYv4h
    callback-url: http://localhost:8080/api/auth/naver/callback

  # Kakao OAuth
  kakao:
    client-id:
    client-secret:
    callback-url: http://localhost:8080/api/auth/kakao/callback

  # NaverPay
  naverpay:
    client-id:
    client-secret:
    chain-id:

  # TossPay
  toss:
    client-key:
    secret-key:
